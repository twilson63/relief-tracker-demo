{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0",
    "express-jwt": "^5.1.0",
    "json-server": "^0.9.0"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 4000 -m ./jwt-check.js"
  }
}
